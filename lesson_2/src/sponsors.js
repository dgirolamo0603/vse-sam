const sponsors = {
    cash: [40000, 5000, 30400, 12000],
    eu: ['SRL', 'PLO', 'J&K'],
    rus: ['RusAuto', 'SBO']
};
function calcCash(...own) {
    var total = own;
    return total;
}
const money = calcCash(null, sponsors.cash);
export {money,sponsors,calcCash};